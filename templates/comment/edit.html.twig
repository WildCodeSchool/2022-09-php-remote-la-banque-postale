{% extends 'base.html.twig' %}

{% block title %}Edit Comment
{% endblock %}

{% block body %}

{% if is_granted("ROLE_ADMIN") %}
	<a class="siteButton" href="{{ path('app_comment_index') }}" title="retour">Retour à la liste des commentaires</a>
{% endif %}


	<div class="marginPage">
		<h1>Éditer le commentaire</h1>
		<div class="blocEdit">
			{{ include('comment/_form.html.twig', {'button_label': 'Sauvegarder'}) }}
			<div class="mt-3">
				{{ include('comment/_delete_form.html.twig') }}
			</div>
		</div>
	</div>
{% endblock %}
