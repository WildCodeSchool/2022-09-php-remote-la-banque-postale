{% extends 'base.html.twig' %}

{% block title %}Comment{% endblock %}

{% block body %}
    <h1>Comment</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ comment.id }}</td>
            </tr>
                        <tr>
                <th>Id du tutoriel</th>
                <td>{{ comment.tutoriel.id }}</td>
            </tr>
            <tr>
                <th>Texte</th>
                <td>{{ comment.text }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_comment_index') }}">Retour à la liste des commentaires</a>

    <a href="{{ path('app_comment_edit', {'id': comment.id}) }}">Éditer le commentaire</a>

    {{ include('comment/_delete_form.html.twig') }}
{% endblock %}
