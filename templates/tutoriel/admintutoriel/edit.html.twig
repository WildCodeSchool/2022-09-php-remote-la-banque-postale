{% extends 'base.html.twig' %}

{% block title %}Edit Tutoriel
{% endblock %}

{% block body %}

	<pre>
	{{ dump(tutoriel.questions.0.content) }}
</pre>

	<a class="siteButton" href="{{ path('app_tutoriel_index') }}" aria-label="retour">Retour à la liste des tutoriels</a>
	<div class="marginPage">
		<h1>Éditer le tutoriel</h1>
		<div
			class="blocEditTuto">
			{# {{ include('tutoriel/admintutoriel/_form.html.twig', {'button_label': 'Modifier'}) }} #}
			{{ component('tutoriel_form', {
				tutoriel: tutoriel,

			}) }}


			<div class="mt-3">
				{{ include('tutoriel/admintutoriel/_delete_form.html.twig') }}
			</div>
		</div>
	</div>
{% endblock %}
