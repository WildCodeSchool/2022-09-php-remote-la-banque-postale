{% extends 'base.html.twig' %}

{% block title %}Edit Question
{% endblock %}

{% block body %}
	<a class="siteButton" href="{{ path('app_question_index') }}" title="retour">Retour à la liste des questions</a>
	<div class="marginPage">
		<h1>Edition de la question</h1>
		<div class="blocEdit">
			{{ include('question/_form.html.twig', {'button_label': 'Sauvegarder'}) }}
			<div class="mt-3">
				{{ include('question/_delete_form.html.twig') }}
			</div>
		</div>
	</div>
{% endblock %}
